# HANDOFF（本地会话交接）

> 用途：在同一台机器上，让另一个 Codex / 终端会话快速接手当前工作状态。
> 新会话开始工作时，先读本文件，再按需读 notes/README.md 和题目目录中的代码。

## 当前进行中

暂无，等待用户指定下一题。

## 刚完成

**32. 最长有效括号（2026-09-04）✅**

- 目录：problems/2026-09-04/0032-longest-valid-parentheses/
- 使用下标栈和虚拟边界 -1；当前实现会累积未匹配右括号的边界下标，但栈顶始终是最近边界，因此结果正确。
- 修复了用 stack<char> 保存下标导致截断，以及把 -1 当作字符串下标访问造成越界的问题。
- 25 个普通固定用例、6 个长度边界用例和 10000 组随机暴力对拍全部通过；严格编译无警告。
- 复盘笔记已写入 notes/2026-09-04/0032-longest-valid-parentheses.md，其中也记录了更常见的标准栈模板。

**295. 数据流的中位数（2026-09-03）✅**

- 目录：problems/2026-09-03/0295-find-median-from-data-stream/
- 使用大顶堆保存较小的一半、小顶堆保存较大的一半，左堆与右堆一样大或多一个元素。
- 从每次全量排序改为双堆；修复了计数漏更新、空堆 top、第二个数的大小分配及重复插入问题。
- 保留用户的分支与 lsize/rsize 计数实现，仅清理未使用变量、注释和格式；addNum 为 O(log n)，findMedian 为 O(1)，空间 O(n)。
- 固定测试、五组每个对象 50000 次调用的大规模测试和 10000 组随机数据流对拍全部通过；严格编译无警告。
- 随机对拍保存在 test.cpp 中，以有序 vector 为独立参考，每次添加后重复查询。
- 复盘笔记已写入 notes/2026-09-03/0295-find-median-from-data-stream.md，包含具体错误案例和“先分配、再平衡”的简化模板。
- 笔记模板已单独通过同一套测试；solution copy.cpp 和 solution_v2.cpp 原样保留为学习历史，默认测试入口仍为 solution.cpp。

**84. 柱状图中最大的矩形（2026-09-03）✅**

- 目录：problems/2026-09-03/0084-largest-rectangle-in-histogram/
- 使用两趟严格递增的单调栈，分别查找每根柱子左右第一个严格更矮的柱子；面积为 heights[i] * (right[i] - left[i] - 1)。
- 栈保存下标；两趟查找均在 >= 时弹栈，无阻挡时边界分别设为 -1 和 n。
- 保留用户的双栈算法，只修正边界说明和注释；时间 O(n)，额外空间 O(n)。
- 23 个固定用例和 10000 组随机暴力对拍全部通过；严格编译无警告。随机对拍保存在 test.cpp 中。
- 复盘笔记已写入 notes/2026-09-03/0084-largest-rectangle-in-histogram.md。

**23. 合并 K 个升序链表（2026-09-02）✅**

- 使用最小堆保存每条非空链表的当前头节点；弹出一个节点后，将其后继节点加入堆。
- 修复了堆为空时直接进入循环、重复加入链表头而未推进链表，以及未补入后继节点的问题。
- 本地 Scaffold 测试编译并运行通过，覆盖官方示例、空链表、重复值、负数和大规模边界输入。
- 当前 `solution.cpp` 使用堆上的虚拟头节点，遗留的一个辅助节点泄漏已记录在复盘笔记中，不影响判题结果。
- 复盘笔记已写入 notes/2026-09-02/0023-merge-k-sorted-lists.md。

**146. LRU 缓存（2026-09-01，2026-09-02 完成验证）✅**

- 使用哈希表和带头尾虚拟节点的双向链表，实现平均 O(1) 的 get、put、顺序更新和 LRU 淘汰。
- 修复了哈希表漏写、移动节点前未摘除、key/value 混淆及 erase(end) 导致的崩溃问题。
- 自定义节点命名为 `DListNode`，避免与 LeetCode 预定义的 `ListNode` 冲突。
- 固定测试和 300 组随机操作序列验证全部通过，严格编译无警告。
- 当前未释放淘汰节点和剩余链表节点，内存管理改进已记录在复盘笔记中。
- 复盘笔记已写入 notes/2026-09-01/0146-lru-cache.md。

**25. K 个一组翻转链表（2026-09-01）✅**

- 使用虚拟头节点和 `groupPrev` 划分每个长度为 k 的分组，迭代翻转区间 `[groupStart, groupNext)`。
- 通过将 `prev` 初始化为 `groupNext`，让翻转后的组尾直接连接下一组；原组头变为组尾后作为下一轮的 `groupPrev`。
- 固定测试和 5000 组随机链表验证全部通过，确认节点身份、节点值、余数分组和无环性质正确。
- 当前保留堆上虚拟头节点写法；固定一个节点的内存泄漏已记录在复盘笔记中，不影响本题判题结果。
- 复盘笔记已写入 notes/2026-09-01/0025-reverse-nodes-in-k-group.md。

**24. 两两交换链表中的节点（2026-08-31）✅**

- 使用栈上虚拟头节点和 pre 指针，迭代交换每一对相邻节点。
- 修复了 first/second 指向同一节点造成的自环，以及交换后未推进 pre 的死循环问题。
- 固定测试和 5000 组随机链表验证全部通过，确认节点身份、节点值和无环性质正确。
- 复盘笔记已写入 notes/2026-08-31/0024-swap-nodes-in-pairs.md。

**41. 缺失的第一个正数（2026-08-29）✅**

- 使用原地哈希 / 下标归位：数字 `x` 尝试放到下标 `x - 1`。
- 通过范围检查和目标位置重复检查避免非法下标与死循环。
- LeetCode 通过，时间复杂度 `O(n)`，额外空间 `O(1)`。
- 复盘笔记已写入 notes/2026-08-29/0041-first-missing-positive.md。

**76. 最小覆盖子串（2026-08-29）✅**

- 当前在家使用 LeetCode 网页端练习，本题未创建本地代码脚手架。
- 使用 `need` / `window` 双哈希表与 `required` / `formed` 状态实现可变长度滑动窗口。
- LeetCode 全部测试通过，复杂度为 `O(|s| + |t|)`。
- 学习过程中补充了 `unordered_map::size()`、`static_cast`、C++ 命名类型转换和 `substr` 越界等语法笔记。
- 复盘笔记已写入 notes/2026-08-29/0076-minimum-window-substring.md。

**98. 验证二叉搜索树（2026-08-26，完成验证于 2026-08-28）✅**

- 目录：problems/2026-08-26/0098-validate-binary-search-tree/
- 使用 long long 上下界递归验证所有祖先约束，严格拒绝重复值。
- 固定测试全部通过。
- 10000 棵随机二叉树与独立的迭代中序遍历参考实现对比全部通过。
- 复盘笔记已写入 notes/2026-08-26/0098-validate-binary-search-tree.md。

**108. 将有序数组转换为二叉搜索树（2026-08-26）✅**

- 使用中点作为根节点，递归构造左右区间。
- 固定测试和 5000 组随机严格递增数组测试全部通过。
- 复盘笔记已写入 notes/2026-08-26/0108-convert-sorted-array-to-bst.md。

**543. 二叉树的直径（2026-08-26）✅**

- 后序 depth 递归，全局 ans = max(ans, 左高度 + 右高度)。
- 固定用例和 2000 棵随机树对拍已通过。
- 复盘笔记已写入 notes/2026-08-26/0543-diameter-of-binary-tree.md。

## Git 状态

- 当前工作分支为 main，远程分支为 origin/main。
- 32 的代码、测试、复盘笔记和索引已经完成；提交和同步状态以 git log、git status 为准。
- 用户已明确要求：每道题完成并更新笔记后，自动执行 git add、commit、push。
- 新题目统一归档到 problems/YYYY-MM-DD/。

## 环境与命令

### 编译 / 运行

g++ 不在默认 PATH 里，要用完整路径：

C:\Users\BHJ4SZH\AppData\Local\Microsoft\WinGet\Packages\BrechtSanders.WinLibs.POSIX.UCRT_Microsoft.Winget.Source_8wekyb3d8bbwe\mingw64\bin\g++.exe

在题目目录中运行：

g++.exe -std=c++17 test.cpp -o test.exe
.\test.exe

### 文件写入

- apply_patch 在此环境可能因 sandbox split-writable-roots 失败。
- 如果 apply_patch 失败，写文件使用 PowerShell 的 UTF-8 无 BOM 方式。
- 测试和程序输出使用英文，避免 Windows 终端编码问题。
- PowerShell 里不要使用 $HOME 作为变量。

## 工作流约定

- problem.md 只放原题面，绝不写提示或解法方向。
- 用户卡住时先给思路和结构，不直接给完整代码。
- 每完成一题：review → 本地测试 → 随机验证 → 更新 notes/YYYY-MM-DD/、notes/README.md 和交接文档。
- 当前在家学习时默认直接使用 LeetCode 网页端，只记录 notes，不创建本地题目脚手架；用户另有要求时除外。
- 当前题目完成后，下一题等待用户指定，不要擅自开始新题。
- 每道题完成且检查无误后，按用户要求自动提交并推送相关变更。

## 下次交接前更新

- [x] 32 已完成并写入复盘笔记，固定测试、长度边界测试和随机对拍通过。
- [x] 295 已完成并写入复盘笔记与简化模板，固定测试、大规模测试和随机对拍通过。
- [x] 84 已完成并写入复盘笔记，固定测试和随机对拍通过。
- [x] 23 已完成并写入复盘笔记。
- [x] 146 已完成并写入复盘笔记。
- [x] 25 已完成并写入复盘笔记。
- [x] 24 已完成并写入复盘笔记。
- [x] 98 已完成并写入复盘笔记。
- [x] 76 已完成并写入复盘笔记，C++ 语法参考文档已更新。
- [x] 41 已完成并写入复盘笔记。
- [x] notes/README.md、handoff.md、HANDOFF.md 已同步。
