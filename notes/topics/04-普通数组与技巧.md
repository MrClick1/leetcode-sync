# 普通数组与技巧

这一组题目利用题目约束建立不变量：有的可以线性扫描或原地处理，有的需要先排序，再进行线性扫描。

## 题目清单

| 题目 | 核心技巧 | 关键易错点 | 复杂度 |
|---|---|---|---|
| [56. 合并区间](../2026-09-22/0056-merge-intervals.md) | 按起点排序，与最后一个结果区间合并 | 重叠用 <=；终点取 max，不能缩短 | O(n log n) 时间 / O(n) 结果空间 |
| [53. 最大子数组和](../2026-09-22/0053-maximum-subarray.md) | 动态规划，维护固定结尾状态与历史最优 | 最终返回 res；全负数不能返回 0 | O(n) 时间 / O(n) 空间，可压缩至 O(1) |
| [41. 缺失的第一个正数](../2026-08-29/0041-first-missing-positive.md) | 原地哈希 / 下标归位 | 条件顺序与重复数字死循环 | `O(n)` / `O(1)` |
| [136. 只出现一次的数字](../2026-08-14/0136-single-number.md) | 全部元素异或 | 理解抵消性质 | `O(n)` / `O(1)` |
| [169. 多数元素](../2026-08-16/0169-majority-element.md) | Boyer–Moore 投票 | 新候选人要获得第一票 | `O(n)` / `O(1)` |

## 56. 合并区间

排序后，新区间只可能与结果的最后一段重叠。若新起点不超过最后一段的终点，则取两个终点的最大值；否则加入新区间。端点相等也算重叠，完全包含时不能直接覆盖终点。

## 41. 缺失的第一个正数


长度为 `n` 时，答案一定在 `[1,n+1]`。把数组本身当作哈希表：数字 `x` 应放在下标 `x-1`。

```cpp
while (nums[i] >= 1 &&
       nums[i] <= n &&
       nums[i] != nums[nums[i] - 1]) {
    swap(nums[i], nums[nums[i] - 1]);
}
```

- 范围检查必须写在数组二次下标访问之前，利用 `&&` 短路保证安全。
- 第三个条件防止重复数字在相同位置之间无限交换。
- 交换后当前位置出现新值，因此要继续使用 `while` 检查。

## 136. 只出现一次的数字

异或性质：

```text
a ^ a = 0
a ^ 0 = a
满足交换律和结合律
```

把所有元素依次异或，成对元素全部抵消，最终只剩出现一次的数字。

## 169. 多数元素

把不同元素两两抵消。多数元素出现次数超过一半，因此最终候选人一定是它：

```text
count == 0 → 选择当前元素作为候选人
当前值等于候选人 → count++
否则 → count--
```

选择新候选人时必须让它获得第一票。题目不保证多数元素存在时，还需要第二次遍历验证。

## 交叉题目

- [31. 下一个排列](../2026-08-16/0031-next-permutation.md)：原地数组变换。
- [75. 颜色分类](../2026-08-16/0075-sort-colors.md)：三指针维护数组分区。
- [128. 最长连续序列](../2026-09-06/0128-longest-consecutive-sequence.md)：普通数组配合哈希集合达到平均 `O(n)`。
