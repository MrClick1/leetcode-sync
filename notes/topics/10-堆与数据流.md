# 堆与数据流

## 共性框架

堆适合反复获取当前最大值或最小值：

```text
top() 读取堆顶
push() 加入元素
pop() 删除堆顶但不返回元素
```

默认 `priority_queue<int>` 是大顶堆；小顶堆需要 `greater<int>`。

## 题目清单

| 题目 | 堆的职责 | 关键易错点 | 复杂度 |
|---|---|---|---|
| [295. 数据流的中位数](../2026-09-03/0295-find-median-from-data-stream.md) | 两个堆维护较小和较大的一半 | 大小平衡、堆顶顺序、平均值溢出 | 插入 `O(log n)` |
| [23. 合并 K 个升序链表](../2026-09-02/0023-merge-k-sorted-lists.md)（交叉） | 保存 K 条链表当前最小节点 | 弹出后加入后继 | `O(N log k)` |

## 295. 数据流的中位数

维护两个堆：

- 大顶堆 `left`：较小的一半，堆顶是左半最大值。
- 小顶堆 `right`：较大的一半，堆顶是右半最小值。

不变量：

```text
left.size() == right.size()
或
left.size() == right.size() + 1
```

因此：

- 总数为奇数：中位数是 `left.top()`。
- 总数为偶数：中位数是两个堆顶的平均值。

求平均值时先转换为 `double` 或更大整数类型，避免两个 `int` 相加溢出。

## 23. 合并 K 个升序链表

小顶堆里保存每条链表当前头节点。每弹出一个最小节点，就把它的后继加入堆。堆大小不超过 `k`，总节点数为 `N`，因此时间复杂度为 `O(N log k)`。

## 小顶堆模板

```cpp
priority_queue<int, vector<int>, greater<int>> minHeap;
```

自定义节点比较器中，返回 `true` 表示左参数优先级更低。要让值小的节点位于堆顶，常写：

```cpp
return left->val > right->val;
```

## 堆题检查清单

- 当前需要快速得到最大值还是最小值？
- 比较器方向是否写反？
- `top()`、`pop()` 前是否检查非空？
- 是否先读取 `top()` 再 `pop()`？
- 插入一个结果后是否需要重新平衡多个堆？
- 堆中保存的是所有元素，还是每个来源的当前候选？
